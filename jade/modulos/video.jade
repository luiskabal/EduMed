ion-view(view-title="Cáncer Colorrectal")
  ion-content
    .content-video
      videogular(vg-player-ready='moduleCtrl.onPlayerReady($API)', ng-show='!moduleCtrl.testRun', vg-theme='moduleCtrl.config.theme.url', vg-complete='moduleCtrl.onCompleteVideo()')
        vg-media(vg-src='moduleCtrl.config.sources', vg-tracks="moduleCtrl.config.tracks", vg-youtube)
        vg-overlay-play
        vg-poster(vg-url='moduleCtrl.config.plugins.poster')
        vg-controls
          vg-play-pause-button
          vg-time-display {{ currentTime | date:'mm:ss':'+0000' }}
          vg-scrub-bar
            vg-scrub-bar-current-time
          vg-time-display {{ timeLeft | date:'mm:ss':'+0000' }}
          vg-time-display {{ totalTime | date:'mm:ss':'+0000' }}
          vg-volume
            vg-mute-button
            vg-volume-bar
          vg-fullscreen-button
        vg-buffering
    div
      .padding
        .box-bg
          ul.module-steps(data-module-steps="{{moduleCtrl.moduleStepCurrent}}")
            li.module-step(ng-repeat="mod in moduleCtrl.videos track by $index", ng-click="moduleCtrl.setVideo({{$index}})")
            //li.module-step(ng-repeat="mod in moduleCtrl.guide.modulos",
                      ng-class="{'step-ok': mod.avance && mod.avance.completado , 'active': mod.avance && !mod.avance.completado }")

          h2.title-view(ng-click="moduleCtrl.showConfirm()") Modulo Introduccion
          .item
            button.button.button-full.button-positive(type='submit', ng-click='openModal()')
              | Comenzar Test
